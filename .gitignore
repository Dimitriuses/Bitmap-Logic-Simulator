node_modules/

# Build output — the Pages workflow runs `npm run build` and publishes the
# result, so compiled JavaScript is never committed.
dist/
_site/

# The original Delphi simulator by Zoltán Hetesi. Kept locally as the reference
# for simulation semantics (UMain.pas), deliberately not redistributed here —
# it is someone else's work, binary included. Not in this repository's history.
BmpLogicSim150902/

*.local
.DS_Store
Thumbs.db

*.claude/skills
